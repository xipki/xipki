TMS-${project.version} revision ${buildNumber} build at ${timestamp}

1. Quickstart

To run the Server in interactive (CLI) mode run:
bin/karaf

To run it in background run:
bin/start
or
bin/stop


2. Data directories
etc/
Configuration files can be found in the 'etc' directory.
Basic configuration for the server can be found in the de.authentidate.*.cfg files.
the etc/org.ops4j.pax.logging.cfg file can be used to configure the logging levels.
