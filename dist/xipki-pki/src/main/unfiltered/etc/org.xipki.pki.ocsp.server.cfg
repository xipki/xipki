# Configuration file
confFile = xipki/ocsp-config/ocsp-responder.xml

# URL patterns of the CMP serice
# The default is /ocsp/*
#urlPatterns = /ocsp/*

# URL patterns of the health check service
# The default is /health/ocsp/*
# healthUrlPatterns = /health/ocsp/*

# Timeout in milli seconds to wait util a certificate status store can be created
# The default is 60000 (one minute)
#newStoreTimeout = 60000
