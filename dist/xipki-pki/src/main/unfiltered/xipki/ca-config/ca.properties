datasource.ca = xipki/ca-config/ca-db.properties
datasource.ocsp = xipki/ca-config/ocsp-db.properties

# master or slave
# the default is master
ca.mode = master

# shard id, between 0 and 255. CA systems using same databases must have different shard id.
ca.shardId = 0
